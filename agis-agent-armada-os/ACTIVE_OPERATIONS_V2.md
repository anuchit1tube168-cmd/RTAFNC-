# AGIS ACTIVE OPERATIONS V2

This package replaces the old redirect-only Active Operations page with a quality-first 12-Agent command center.

## Canonical agents
See `agents/core-v2/`.

## Public page
`/active-operations.html`

## Legacy
The former Fleet RPG v6 under `apps/command-center-v3/` remains preserved as legacy/reference.

## Design rule
Do not present animation as proof of execution. UI state must distinguish demo/ready/blocked from verified live runtime.
